{
  "additionalProperties": false,
  "description": "Identity established by the Controller, never populated by the model.",
  "properties": {
    "principal": {
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:@/-]{0,127}$",
      "title": "Principal",
      "type": "string"
    },
    "permissions": {
      "items": {
        "type": "string"
      },
      "maxItems": 32,
      "title": "Permissions",
      "type": "array"
    }
  },
  "required": [
    "principal"
  ],
  "title": "SessionAgentSubject",
  "type": "object"
}