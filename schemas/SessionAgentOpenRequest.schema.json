{
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "rolo-session-agent-open-request/v1",
      "default": "rolo-session-agent-open-request/v1",
      "title": "Schema Version",
      "type": "string"
    },
    "allowed_target_ids": {
      "items": {
        "type": "string"
      },
      "maxItems": 1000,
      "minItems": 1,
      "title": "Allowed Target Ids",
      "type": "array"
    },
    "max_tool_calls": {
      "default": 4,
      "maximum": 8,
      "minimum": 1,
      "title": "Max Tool Calls",
      "type": "integer"
    },
    "timeout_s": {
      "default": 120,
      "maximum": 1800,
      "minimum": 10,
      "title": "Timeout S",
      "type": "integer"
    },
    "conversation_sha256": {
      "anyOf": [
        {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Conversation Sha256"
    }
  },
  "required": [
    "allowed_target_ids"
  ],
  "title": "SessionAgentOpenRequest",
  "type": "object"
}