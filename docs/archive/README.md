# 归档文档

这里保存已完成、被替代或只适用于一次性环境的计划与验证材料。它们用于追溯背景，
不作为当前实现、CLI 入口或运行契约的规范来源。

当前工程的文档入口请从 [docs/README.md](../README.md) 开始；面向用户的本地 Adapt
首选入口是 `rolo adapt <本地工作区> --robot <机器人 ID>`，需要远程证据或专家参数时
再使用 `robotctl adapt start`。
