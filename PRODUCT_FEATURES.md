# rolo 产品特性

**rolo** 是一个面向真实机器人自主调试与测试的监督系统。它把每次机器人运行组织成一个边界清晰的闭环：定义任务、执行动作、持续观察、判断结果、保留证据，并根据新的证据决定下一次运行。

## 1. 一次运行，一个完整证据闭环

rolo 不把“命令成功返回”视为任务完成。一次用例的执行会留下一个可以回放、解释和复现的完整 episode，并同步记录：

- 下发给机器人的命令；
- 机器人实际执行状态；
- 传感器和系统遥测；
- 相机画面与任务现场变化；
- 参数、软件和能力配置版本；
- 测试判定、异常区间和诊断结论。


## 2. 标准CLI

在 rolo 中，异构机器人不同的自由度、执行器、传感器和计算平台，都统一为标准 CLI 对外提供服务。rolo 将机器人软件栈划分为四层，上层 Agent 面对的是统一的命令格式、单位、坐标系、时间戳、错误码和回滚语义：

- **Hardware**：传感器、执行器、总线、固件、电源和硬件状态；
- **Linux**：进程、服务、网络、资源、文件和系统性能；
- **Middleware**：节点、Topic、Service、Action、TF、参数和诊断信息；
- **Application**：建图、定位、导航、操作、测试、调优和任务状态。


## 3. 主动发现

rolo 首次部署时，为机器人分配唯一的 `robot_id`，通过主动发现生成标准化的 capability manifest、语义绑定候选和 CLI tool catalog：

- 计算平台和系统版本；
- 已连接的传感器、执行器和通信总线；
- Linux 服务、进程和运行依赖；
- ROS 发行版、节点、Topic、TF 和接口；
- 本地源码工程、可执行程序和 README；
- 已有厂商 CLI、Middleware CLI 和应用入口。


## 4. robot_use

特有的`robot_use` 模式，允许机器人自主调用本体或第三视角相机如VICON，将带时间戳的关键帧序列与任务状态、控制命令、里程计和遥测数据组合起来，进行多模态分析：

- 低频周期监督；
- 任务状态变化时立即触发；
- 测试步骤结束后的强制验证；
- 异常遥测触发的高频监督；
- 重叠时间窗和多帧时序理解；
- 未知错误时刻的粗到细录像回溯。


## 5. 自主测试

用户可以用结构化约束描述期望，例如：

- 最大速度和加速度；
- 允许的目标误差；
- 最小障碍物距离；
- 禁止进入的区域；
- 最长任务时间；
- 允许使用的传感器；
- 网络中断、节点重启或低电量条件；
- 需要完成的导航和操作任务。

rolo 检查约束是否明确、可执行、可观测，对于可接纳的约束生成：

- 覆盖矩阵；
- 正常、边界和异常测试；
- 状态转换测试；
- 组合测试；
- 变形测试；
- 故障注入测试；
- 每个用例对应的测试 Oracle；
- 风险等级和执行顺序。


## 6. 自主调优

rolo 将算法参数暴露为统一的参数注册表，每次调优都遵循“建立基线—生成候选—受控试运行—评估—回归—固化或回滚”的生命周期：

- 标准化名称和单位；
- 当前值、默认值和允许范围；
- 参数类型和修改方式；
- 与其他参数的依赖关系；
- 修改后需要重启或重新标定的组件；
- 风险等级和回滚方法；
- 适用的机器人能力和算法实现。


## 7. 自治与可审计

rolo 使用状态图管理发现、标定、操作、建图、定位、导航、测试、诊断、调优和回归过程。长时间任务可以生成check point，记录当前配置、活动任务、测试进度和证据索引。多台机器人可以运行相同的 Agent 和测试 DSL，同时保持各自独立的身份、参数、状态和证据。